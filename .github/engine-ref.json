{
  "ref": "5e38a875000a111c1038d7f8bd41146d8b3ad346",
  "branch": "main",
  "why": "本次升级：**为让 `books#27`（fruit-demo 散文 0 宏）用上两笔作者面能力** —— `#1408`（`links[]` 的 `gives`/`sets`/`yields` 随链接、**点击那一刻**施加 ⇒ 编进 `<a data-sg-effects>`）＋ `#1472`（规则行效果**编译期就编进链接** ✗ 不再渲染期落）。旧 pin `343578d51` **两笔都不含** ⇒ 本笔把 11 条规则行搬进 `links[]` 后，旧 pin 下 `links` 的 `gives`/`sets` **不生效** ⇒ 段里**不会出现**「吃掉苹果」这类行 ⇒ 4 个用例红（`✗ 找不到链接「吃掉苹果」`）。★证据（可复跑 · 双向对照 · 两棵树皆**先清三层**）：① 旧 pin `343578d51` ⇒ `case-run --slug=fruit-demo` ⇒ **绿 1 ／ 未归因 4 ⇒ rc=1**（复现 CI 同形红）；② 新 pin `5e38a8750` ⇒ 同法 ⇒ **绿 5 ⇒ rc=0** ✓。③ 祖先性（权威＝**merge 结果** ✗ 不是 PR 头）：`git merge-base --is-ancestor 5dccca9 5e38a8750` ⇒ 真（`#1472` 的 merge）；`053d286`（`#1408`）亦为真 ⇒ 两笔皆在该 pin 上，且该 pin 在引擎 main ✓。",
  "checked": "本地核过（本笔）：① 新 pin 树对**本笔故事**（fruit-demo）：`build` rc=0；`case-run --slug=fruit-demo` ⇒ **5 条全绿 ⇒ rc=0** ✓；② 新 pin 树对**本仓全部故事**：`build` rc=0（★输出含 `#1128 产物重建：<slug>` 证明**真重编**）；`case-run` ⇒ **28 条：绿 27 ＋ 预期缺口 1（`#1236`）⇒ rc=0** ✓；③ ★两棵树都**先清三层**（`dist`／`build`／**故事树里的 `1[5678]-*.twee` 与 `00-meta.twee`**）再跑 —— 否则 build 见生成物已在会**跳过重编** ⇒ 读到旧产物 ⇒ **假读数**（本笔实测踩到：未清时 8 条未归因，清后 0 条 ✓）。"
}
