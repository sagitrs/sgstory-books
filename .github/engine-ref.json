{
 "ref": "17fb6b2a8ac356f314e39e4010304825a78c18f7",
 "branch": "main",
 "why": "本仓（books）发布/门跑时使用的引擎版本。★ 钉**完整 sha**（✗ 不用 tag／分支名，理由同 `#1262`：只有完整 sha 是可复现凭据）。升级＝改这一行 ＋ 在本仓 PR 里写清为什么。｜本次升级：**为接\"故事面判据\"到 PR 门**（`#1401` 可达性机制 `unreachablePassages` 随 `#1411` 落地；散文词汇面 `prose-vocabulary` 的 **P5** 随 `#1409` 修复落地）—— 旧 pin `c8b3a3c` 里**没有** `unreachablePassages` 导出 ⇒ 门接线会 `SyntaxError` ✗（我实测撞到）⇒ 必须同笔升 pin ✓。",
 "checked": "本地核过（本笔）：`SG_STORIES_DIR=<本仓>/stories node build.mjs`（引擎＝新 pin `17fb6b2a`）⇒ rc=0；`node test/prose-vocabulary.mjs` ⇒ 改制面 P1–P5 命中 0 项（`#19` 修后）；★可达性（`unreachablePassages`，两处出边并集）⇒ 对 4 个故事实测：改制面 2 个（`m3-min-new`／`mist-forest`）全可达 ✓、旧形态 2 个不判 ✓。"
}
