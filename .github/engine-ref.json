{
  "ref": "a5b0529a69e6fef89b04bde6f2c39499270c84d8",
  "branch": "main",
  "why": "本次升级：**为让 `books` 的 fruit-demo 用上 `takes`（库存移除）** —— `#1471`/`#1475` 补的效果面第二形（`takes: [<道具名>…]` ＝ `delete pc.inv[k]`，✗ 不置 `false`）。旧 pin `5e38a875000a111c1038d7f8bd41146d8b3ad346` **不含** `takes` ⇒ 本笔给五条\"吃掉X\"加 `takes` 后，**旧 pin 下效果静默不生效**（✗ 门却绿 ⇒ 假绿）✓。★证据（可复跑）：① 祖先性 `git merge-base --is-ancestor <本次 pin> origin/main` ⇒ 真；② `git grep -c applyTakes <本次 pin> -- src/engine/40-sim/22-rules.twee` ⇒ **1**（旧 pin ⇒ **0**）；③ 行为：用本次 pin 的树编 books ⇒ 点「收下苹果」⇒ `pc.inv = {苹果:true}`；点「吃掉苹果」⇒ **`pc.inv = {}`**（★苹果消失 ✓）。★★读数注意（我实测踩到）：**必须每次重读 `State.variables.pc`** —— SugarCube **跳转时会换对象**，缓存引用会让我读到**旧对象**（我第一次就因此误报「takes 没生效」✗）。",
  "checked": "本地核过（本笔）：① 用**本次 pin 的树**编 books 全故事 ⇒ `build` rc=0；`case-run` ⇒ **28 条：绿 27 ＋ 预期缺口 1（`#1236`）⇒ rc=0** ✓；② ★fruit-demo 五条用例**全绿**（含 `eat-then-exit`／`full-chain`）✓；③ ★两棵树都**先清三层**（`dist`／`build`／★**故事树里的 `1[5678]-*.twee` 与 `00-meta.twee`**）再跑 —— 否则 build 见生成物已在会**跳过重编** ⇒ 读到旧产物 ⇒ **假读数** ✓。"
}
