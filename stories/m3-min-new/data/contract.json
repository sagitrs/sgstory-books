{
 "section": "StoryBindings",
 "note": "最小新格式（`m3-min-new`）的接入契约成员：照最小合法集；本故事不新增面。",
 "members": [
  {
   "name": "rules",
   "kind": "empty-array",
   "docs": "条件表：本故事**没有** `data/rules.json` ⇒ 缺省＝**空表**；链接由 `passages.json` 的 `links[]` 合成进规则行 ✓"
  },
  {
   "name": "mechanics",
   "kind": "null",
   "docs": "新机制声明表：本故事未启用 ⇒ 按 `#492` 口径**也要显式声明**（`null` ＝ 能力不在 ✓）"
  }
 ]
}
